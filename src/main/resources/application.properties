# Server configuration
server.port=${PORT:8080}

# Database configuration - TEMPORARY HARDCODED (use the exact credentials from your screenshot)
spring.datasource.url=jdbc:postgresql://dpg-d3fn6bp5pdvs73be510g-a.oregon-postgres.render.com:5432/roth
spring.datasource.username=roth_user
spring.datasource.password=ArTLhsMDelJWYbabV93WhrQI60g5rZUL

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Health checks
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# Production settings
spring.jpa.properties.hibernate.jdbc.time_zone=UTC